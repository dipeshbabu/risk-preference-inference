# Machine-readable interfaces

The JSON schemas define the public benchmark manifest and the route returned by
an evaluated method. Semantic checks such as policy membership and pilot-budget
accounting are implemented in `risk_shift_bench.submission_protocol`.
